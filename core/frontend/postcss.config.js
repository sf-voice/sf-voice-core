// tailwind v4 ships its own postcss plugin. no tailwind.config.js needed —
// theme + content config live as directives inside src/index.css.
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
