# mix build / dep dirs — at any depth (root + per-app)
_build/
cover/
deps/
doc/
docs/
.fetch

# vm crash dumps
erl_crash.dump

# archive artifacts (mix archive.build)
*.ez

# tmp scratch
/tmp/

# package tarballs
*-*.tar

# phoenix asset build output (lives inside each app's priv/static)
priv/static/assets/
priv/static/cache_manifest.json

# node
npm-debug.log
node_modules/

# Database files
*.db
*.db-*

# ElixirLS language server cache
.elixir_ls/

# macOS metadata
.DS_Store

# Local env files (keep an example if you add one)
.env
.env.*
!.env.example

# Stray compiled bytecode (normally inside _build/)
*.beam

# Editor scratch files
*.swp
*~
.idea/
.vscode/

# multi-region planning doc — working draft, not for the public repo yet.
# move into docs/ and unignore once the design lands.
multi-region-support.md
