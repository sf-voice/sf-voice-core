---
title: "Démarrage rapide"
description: "De la clé API au premier résultat de recherche."
---

Installez le SDK TypeScript, ingérez un enregistrement, attendez son indexation, puis lancez une recherche.

## Installation

<CodeGroup>
```sh pnpm
pnpm add @sf-voice/media@0.1.1
```

```sh npm
npm install @sf-voice/media@0.1.1
```

```sh bun
bun add @sf-voice/media@0.1.1
```
</CodeGroup>

## Créer un client

```ts
import { SfVoiceMedia } from "@sf-voice/media";

const client = new SfVoiceMedia({
  baseUrl: "https://api.sf-voice.com",
  apiKey: process.env.SF_VOICE_API_KEY!,
});
```

## Ingérer un enregistrement

Passez votre propre `asset_id` — c'est ainsi que vous identifierez l'enregistrement dans les résultats de recherche. Utilisez `asset_class` pour regrouper les enregistrements appartenant au même client ou workspace.

```ts
const { task_id } = await client.ingest({
  source: "url",
  asset_id: "call_001",
  asset_class: "customer_acme",
  url: "https://example.com/call.mp3",
  media_type: "audio",
  types: ["audio", "transcript"],
});
```

## Attendre l'indexation

```ts
const task = await client.pollTask(task_id, {
  intervalMs: 2_000,
  timeoutMs: 120_000,
});

if (task.status === "failed") {
  throw new Error(task.error ?? "indexing failed");
}
```

## Rechercher

```ts
const { results } = await client.search({
  query: "customer asks about pricing",
  asset_class: "customer_acme",
  types: ["transcript"],
  threshold: 0.7,
});

for (const r of results) {
  console.log(r.asset_id, r.start_ms, r.end_ms, r.score);
}
```

Les résultats incluent `start_ms` et `end_ms` pour vous permettre de positionner un lecteur sur le segment correspondant.

## Exemple complet

```ts
import { SfVoiceMedia, SfVoiceMediaError } from "@sf-voice/media";

const client = new SfVoiceMedia({
  baseUrl: "https://api.sf-voice.com",
  apiKey: process.env.SF_VOICE_API_KEY!,
});

try {
  const { task_id } = await client.ingest({
    source: "url",
    asset_id: "call_001",
    asset_class: "customer_acme",
    url: "https://example.com/call.mp3",
    media_type: "audio",
    types: ["audio", "transcript"],
  });

  const task = await client.pollTask(task_id);

  if (task.status === "failed") {
    throw new Error(task.error ?? "indexing failed");
  }

  const { results } = await client.search({
    query: "customer asks about pricing",
    asset_class: "customer_acme",
  });

  console.log(results);
} catch (err) {
  if (err instanceof SfVoiceMediaError) {
    console.error(err.code, err.status, err.message);
  }
  throw err;
}
```

## Prochaines étapes

<CardGroup cols={2}>
  <Card title="Fonctionnement" icon="workflow" href="/how-it-works">
    Comprendre le cycle de vie ingestion → indexation → recherche.
  </Card>
  <Card title="SDK TypeScript" icon="square-js" href="/sdks/typescript">
    Entrées, sorties et types d'erreurs.
  </Card>
</CardGroup>
