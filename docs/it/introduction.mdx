---
title: "Introduzione"
description: "API e SDK di ricerca multimediale per prodotti di IA vocale."
---

San Francisco Voice Company sviluppa API e SDK di ricerca multimediale per prodotti di IA vocale.

La nostra prima funzionalità è un motore di ricerca per i media. Inserisci audio o video, eseguine la ricerca in linguaggio naturale e usa i timestamp restituiti per tornare al punto preciso nella registrazione. Non è necessario costruire prima il proprio archivio di trascrizioni, pipeline di embedding o livello di ricerca multimediale.

<Warning>
  Questa documentazione copre la versione `0.1.1`. Stiamo iterando rapidamente, quindi fissa la versione dell'SDK e aspettati cambiamenti incompatibili tra le versioni finché l'API non si stabilizza.
</Warning>

La ricerca è il nostro punto di partenza. Poi seguiranno strumenti di benchmarking e debugging per i sistemi di IA vocale.

## Cosa puoi costruire

- Cercare le chiamate dei clienti per argomento, obiezione o richiesta di funzionalità.
- Trovare le chiamate in cui la tua IA ha interrotto l'interlocutore, ha parlato troppo lentamente o si è ripetuta.
- Limitare ogni cliente finale alle sue chiamate.
- Costruire flussi di revisione di conformità, processi di QA o basi di conoscenza sopra il tuo archivio di chiamate.

## Il modello centrale

Gli asset portano ID e ambito dal tuo sistema.

| Campo | Cosa è |
| --- | --- |
| `asset_id` | Il tuo ID per la registrazione. Obbligatorio. Restituito nei risultati di ricerca per consentirti di collegare al tuo sistema. |
| `asset_class` | Un gruppo logico — un cliente, un workspace, un progetto. Limita la ricerca affinché i risultati non sconfinino oltre i confini. |
| `types` | Cosa indicizzare: `audio`, `video`, `transcript` o qualsiasi combinazione. |
| `metadata` | Coppie chiave/valore piatte per correlazione e visualizzazione. |

## Ciclo di vita

<Steps>
  <Step title="Ingerire">
    Invia un URL, una chiave S3 o un file con il tuo `asset_id`. L'API restituisce immediatamente un `task_id`.
  </Step>
  <Step title="Indicizzare">
    Interroga il task finché raggiunge `ready`. L'indicizzazione avviene in background.
  </Step>
  <Step title="Cercare">
    Esegui query in linguaggio naturale, limitate a una `asset_class` o ad `asset_ids` espliciti. I risultati includono `start_ms` e `end_ms`.
  </Step>
</Steps>

## Dati e privacy

Indicizziamo i tuoi media e conserviamo la rappresentazione ricercabile: embedding, trascrizioni e metadati. I file sorgente transitano attraverso la pipeline di elaborazione e non vengono conservati al termine dell'indicizzazione.

**Tu mantieni la proprietà dei tuoi media. Noi conserviamo i dati indicizzati necessari per la ricerca.**

Elaborazione regionale:
- **Clienti USA** — elaborati negli Stati Uniti.
- **Clienti UE e UK** — elaborati in Europa.

<Card title="Dati e privacy" icon="shield" href="/data-privacy">
  Gestione dei dati, conservazione, elaborazione regionale e dettagli sui DPA.
</Card>

## SDK

Otto SDK, con client specifici per linguaggio per la stessa API media.

<CardGroup cols={4}>
  <Card title="TypeScript" icon="square-js" href="/sdks/typescript" />
  <Card title="Python" icon="python" href="/sdks/python" />
  <Card title="Elixir" icon="code" href="/sdks/elixir" />
  <Card title="Go" icon="code" href="/sdks/go" />
  <Card title="Rust" icon="gear" href="/sdks/rust" />
  <Card title="Java" icon="java" href="/sdks/java" />
  <Card title="Kotlin" icon="code" href="/sdks/kotlin" />
  <Card title="C++" icon="code" href="/sdks/cpp" />
</CardGroup>

Gli SDK sono open source: [github.com/sf-voice/sf-voice-core](https://github.com/sf-voice/sf-voice-core)

## Inizia da qui

<Card title="Avvio rapido" icon="rocket" href="/quickstart">
  Inserisci una registrazione, attendi l'indicizzazione, esegui la tua prima ricerca.
</Card>
